import styled from 'styled-components';

import {
  space,
  color,
  layout,
  boxShadow,
  border,
  flexbox,
} from 'styled-system';

export const Box = styled('div')(
  space,
  color,
  layout,
  boxShadow,
  border,
  flexbox
);
